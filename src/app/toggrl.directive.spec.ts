import { ToggrlDirective } from './toggrl.directive';

describe('ToggrlDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggrlDirective();
    expect(directive).toBeTruthy();
  });
});
